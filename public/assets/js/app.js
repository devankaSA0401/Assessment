document.addEventListener('change', e => { if(e.target.matches('.status-select')) { const form=e.target.closest('form'); const fields=form.querySelector('.conditional-fields'); fields.classList.toggle('visible', ['Non-Compliant','Partially Compliant'].includes(e.target.value)); }});
document.querySelectorAll('form[data-autosave]').forEach(form => { form.addEventListener('submit', async e => { if(!window.fetch) return; e.preventDefault(); const state=form.querySelector('.save-state'); state.textContent='Saving…'; try { const res=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{'X-Requested-With':'XMLHttpRequest'}}); const data=await res.json(); state.textContent=data.ok?'Saved ✓':'Save failed'; if(data.ok) setTimeout(()=>state.textContent='',1600); } catch(err){ state.textContent='Save failed'; } }); });
